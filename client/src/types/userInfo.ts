export type UserInfo = {
    name: string;
    email: string;
    password: string;
    confirmPassword: string
}

export type UserData = {
    name: string;
    email: string;
    isActivated: boolean;
}